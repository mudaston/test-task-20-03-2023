import React, { FC } from 'react'

import { Logo } from './Logo'

interface LogoProps {}

type Props = LogoProps

const Component: FC<Props> = ({}) => {
	return <Logo>Twitter</Logo>
}

export default Component
